[project]
name = "en-vi-neural-machine-translation"
version = "0.1.0"
description = "English-to-Vietnamese neural machine translation: BiGRU encoder + Bahdanau attention decoder trained from scratch in PyTorch on the IWSLT'15 EN-VI corpus"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
